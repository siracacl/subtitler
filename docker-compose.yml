services:
  subtitler:
    build: .
    ports:
      - "8642:8642"
    # Privileged mode needed for mounting SMB/CIFS shares inside the container.
    # cap_add: SYS_ADMIN alone fails on some platforms (Swarm, Portainer, NAS).
    privileged: true
    env_file: stack.env
